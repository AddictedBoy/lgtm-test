apiVersion: apps/v1
kind: Deployment
metadata:
  name: sample-logs-app
  namespace: observability
  labels:
    app: sample-logs-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sample-logs-app
  template:
    metadata:
      labels:
        app: sample-logs-app
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8091"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: sample-logs-app
          image: sample-logs-app:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8091
              name: http
          env:
            - name: PORT
              value: "8091"
            - name: SERVICE_NAME
              value: "sample-logs-app"
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8091
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8091
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: sample-logs-app
  namespace: observability
spec:
  type: ClusterIP
  ports:
    - port: 8091
      targetPort: 8091
      protocol: TCP
      name: http
  selector:
    app: sample-logs-app
